# Local Keycloak Configuration (Docker)
# Use this for local development with dockerized Keycloak
# To use: copy to .env or set environment variables manually

# Keycloak URLs (local Docker instance)
KEYCLOAK_BASE_URL=http://localhost:8085
KEYCLOAK_ISSUER_URI=http://localhost:8085/realms/kymatic
KEYCLOAK_REALM=kymatic

# Keycloak Admin Credentials (for service integration)
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Internal service-to-service communication (within Docker network)
KEYCLOAK_INTERNAL_URL=http://keycloak:8080



